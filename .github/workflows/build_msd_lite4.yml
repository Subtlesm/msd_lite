name: 编译 AX3600 专用 msd_lite (Final)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 1. 拉取当前工作流仓库
      uses: actions/checkout@v4

    - name: 2. 安装编译器和 CMake
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu build-essential cmake

    - name: 3. 下载 msd_lite 源码
      run: |
        # 必须加 --recursive 下载子模块
        git clone --recursive https://github.com/rozhuk-im/msd_lite.git

    - name: 4. 生成交叉编译配置
      run: |
        cd msd_lite
        cat <<EOF > toolchain.cmake
        set(CMAKE_SYSTEM_NAME Linux)
        set(CMAKE_SYSTEM_PROCESSOR aarch64)
        set(CMAKE_C_COMPILER aarch64-linux-gnu-gcc)
        set(CMAKE_CXX_COMPILER aarch64-linux-gnu-g++)
        set(CMAKE_FIND_ROOT_PATH /usr/aarch64-linux-gnu)
        set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
        set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
        set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
        set(PTHREAD_LIBRARY pthread CACHE STRING "Forced pthread" FORCE)
        set(CMAKE_USE_PTHREADS_INIT 1)
        EOF

    - name: 5. 编译
      run: |
        cd msd_lite
        mkdir build
        cd build
        
        cmake -DCMAKE_TOOLCHAIN_FILE=../toolchain.cmake \
              -DCMAKE_C_FLAGS="-O3 -mcpu=cortex-a53 -Wall -D_GNU_SOURCE -static" \
              -DCMAKE_EXE_LINKER_FLAGS="-static" \
              -DCMAKE_BUILD_TYPE=Release ..
        
        make -j$(nproc)

    - name: 6. 验证与打包 (关键修复：精确查找)
      run: |
        echo "正在 build 目录中查找二进制文件..."
        
        # 只在 build 目录下找，忽略源码目录里的同名脚本
        # 通常路径是 msd_lite/build/src/msd_lite
        TARGET_FILE=$(find msd_lite/build -name "msd_lite" -type f | head -n 1)
        
        if [ -z "$TARGET_FILE" ]; then
          echo "❌ 错误：在 build 目录下没找到编译好的文件！"
          exit 1
        fi
        
        echo "✅ 找到文件：$TARGET_FILE"
        
        # 再次确认它是 ELF 二进制文件，而不是 shell 脚本
        FILE_TYPE=$(file "$TARGET_FILE")
        echo "文件类型：$FILE_TYPE"
        
        if [[ "$FILE_TYPE" != *"ELF"* ]]; then
          echo "❌ 错误：找到的文件不是二进制程序！"
          exit 1
        fi
        
        # 瘦身
        aarch64-linux-gnu-strip "$TARGET_FILE"
        
        # 复制出来准备上传
        cp "$TARGET_FILE" ./msd_lite_ax3600

    - name: 7. 上传成品
      uses: actions/upload-artifact@v4
      with:
        name: msd_lite-ax3600-final
        path: ./msd_lite_ax3600
