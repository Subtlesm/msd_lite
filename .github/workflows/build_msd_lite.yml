name: 编译 AX3600 专用 msd_lite (Toolchain版)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 1. 拉取仓库
      uses: actions/checkout@v4

    - name: 2. 安装编译器和 CMake
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu build-essential cmake

    - name: 3. 下载 msd_lite 源码
      run: |
        git clone https://github.com/rozhuk-im/msd_lite.git

    - name: 4. 生成交叉编译工具链文件 (解决环境报错的关键)
      run: |
        cd msd_lite
        # 创建一个标准的 toolchain.cmake 文件
        # 它可以让 CMake 准确找到库，不再报错 NOTFOUND
        cat <<EOF > toolchain.cmake
        set(CMAKE_SYSTEM_NAME Linux)
        set(CMAKE_SYSTEM_PROCESSOR aarch64)
        set(CMAKE_C_COMPILER aarch64-linux-gnu-gcc)
        set(CMAKE_CXX_COMPILER aarch64-linux-gnu-g++)
        
        # 告诉 CMake 库文件在哪里 (非常重要)
        set(CMAKE_FIND_ROOT_PATH /usr/aarch64-linux-gnu)
        set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
        set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
        set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
        
        # 暴力修复 PTHREAD_LIBRARY NOTFOUND 错误
        # 直接告诉 CMake：线程库叫 pthread，别去搜索了
        set(PTHREAD_LIBRARY pthread CACHE STRING "Forced pthread" FORCE)
        set(CMAKE_USE_PTHREADS_INIT 1)
        EOF

    - name: 5. 编译 (使用工具链)
      run: |
        cd msd_lite
        mkdir build
        cd build
        
        # 加载刚才生成的 toolchain.cmake
        # -static 放在链接参数里，避免干扰检测
        cmake -DCMAKE_TOOLCHAIN_FILE=../toolchain.cmake \
              -DCMAKE_C_FLAGS="-O3 -mcpu=cortex-a53 -Wall -D_GNU_SOURCE -static" \
              -DCMAKE_EXE_LINKER_FLAGS="-static" \
              -DCMAKE_BUILD_TYPE=Release ..
        
        make -j$(nproc)

    - name: 6. 验证与打包
      run: |
        # 查找文件
        TARGET_FILE=$(find . -name "msd_lite" -type f | head -n 1)
        
        if [ -z "$TARGET_FILE" ]; then
          echo "❌ 依然失败... 请检查日志"
          exit 1
        fi
        
        echo "✅ 编译成功：$TARGET_FILE"
        file "$TARGET_FILE"
        
        # 瘦身并改名
        aarch64-linux-gnu-strip "$TARGET_FILE"
        cp "$TARGET_FILE" ./msd_lite_ax3600

    - name: 7. 上传成品
      uses: actions/upload-artifact@v4
      with:
        name: msd_lite-ax3600-final
        path: msd_lite/build/msd_lite_ax3600
